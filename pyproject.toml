[project]
name = "ha-smartshift"
version = "1.0.0"
description = "DIY battery automation using spot prices and Home Assistant"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-cov>=7.1.0",
]
